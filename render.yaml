# Optional Render Blueprint — import repo in Render UI and adjust names/env as needed.
services:
  - type: web
    name: sync-work-api
    runtime: node
    rootDir: server
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
